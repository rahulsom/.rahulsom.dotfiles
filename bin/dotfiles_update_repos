#!/bin/bash

function dotfiles() {
  files+=($(find "$HOME" -maxdepth 1 -type d -name '.*dotfiles*'))
  echo "${files[@]}"
}

while read d; do
  cd "$d"
  git pull
done < <(dotfiles | xargs -n 1 echo)
