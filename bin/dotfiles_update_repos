#!/bin/bash

while read d; do
  tput bold
  echo -e "$d" | perl -pe "s:$HOME:~:g"
  tput sgr0
  cd "$d" || return
  test -d .git && git pull || echo "Not a git repo"
done < <(ls -1d "$HOME"/.*dotfiles)
