#!/bin/bash
git tag --format='%(creatordate:short)%09%(refname:strip=2)%09%(subject)' --sort=creatordate \
      | column -t -s $'\t' \
      | awk '/rc/
          {printf("\033[%dm%s\033[0m\n",30,$0);next}
          {print}
        '
